@startuml
start
:User opens frontend (React);
:Frontend calls DRF API;
if (Authorized?) then (yes)
  :Validate JWT token;
  :Execute business logic;
  :Read/write PostgreSQL;
  :Return JSON response;
else (no)
  :Return 401;
endif
:Render result in UI;
stop
@enduml
